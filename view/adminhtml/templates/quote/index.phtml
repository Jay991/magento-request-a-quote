<div>
    <h1>Request for Quote</h1>
    
    <table class="quote-table">
        <thead>
            <tr>
                <th>Quote ID</th>
                <th>Customer Name</th>
                <th>Created At</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($block->getQuotes() as $quote): ?>
            <tr>
                <td><?php echo $quote->getQuoteId() ?></td>
                <td><?php echo $quote->getCustomerName() ?></td>
                <td><?php echo $quote->getCreatedAt() ?></td>
                <td>
                    <a href="<?php echo $block->getQuoteEditUrl($quote->getQuoteId()) ?>">Edit</a>
                    <a href="<?php echo $block->getQuoteDeleteUrl($quote->getQuoteId()) ?>">Delete</a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
    
    <div class="actions-toolbar">
        <div class="secondary">
            <a href="<?php echo $block->getCreateQuoteUrl() ?>" class="action add" title="<?php echo __('Create New Quote') ?>">
                <span><?php echo __('Create New Quote') ?></span>
            </a>
        </div>
    </div>
</div>
